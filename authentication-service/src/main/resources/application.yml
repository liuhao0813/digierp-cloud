server:
  port: 8000
spring:
  thymeleaf:
    cache: false
  cloud:
    nacos:
      discovery:
        group: DIGIERP_GROUP
        namespace: ${spring.profiles.active}
        server-addr: localhost:8848
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://47.99.219.21:3306/security?characterEncoding=utf8&&autoReconnect=true

digierp:
  authentication:
#    login-page: demo-login   # 配置自定义登录页面